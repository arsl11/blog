const NavBar = () => {
    return(
        <div>
            <a href="/profile">Profile</a>
            <a href="/dialogs">Dialogs</a>
            <a href='/users'>Users</a>
        </div>
    );
}

export default NavBar;
